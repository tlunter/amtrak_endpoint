$LOAD_PATH << File.join(Dir.pwd, 'lib')
$LOAD_PATH << Dir.pwd

source 'https://rubygems.org'

gem 'amtrak', '~> 1.2'
gem 'gcm'
gem 'shotgun'
gem 'sinatra'
gem 'unicorn'
gem 'redis'
gem 'traceview'
gem 'rollbar', '~> 1.5.0'
gem 'redis-objects'
gem 'connection_pool'

group :development do
  gem 'pry'
end
